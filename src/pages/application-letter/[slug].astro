---
import { enSlugStaticPaths } from "@lib/i18n";

export const getStaticPaths = () => enSlugStaticPaths("applicationLetters");

return Astro.rewrite(`/en/application-letter/${Astro.params.slug}`);
---
