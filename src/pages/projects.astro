---
import BaseLayout from "@layouts/BaseLayout.astro";
import HorizontalCard from "@components/HorizontalCard.astro";
import { projects } from "@lib/data/projects";

const prefetchUrls = ["/", "/cv"];
const includeSidebar = true;
---

<BaseLayout
    class="prose prose-sm md:prose-base space-y-8"
    title="Projects"
    description="List of all projects Hans Askov has worked on"
    theme="light"
    {prefetchUrls}
    {includeSidebar}
>
    <h1 class="text-3xl lg:text-5xl font-bold not-prose">My projects</h1>
    {
        projects.map((project, i) => (
            <>
                {i > 0 && <div class="divider" />}
                <HorizontalCard {...project} />
            </>
        ))
    }
</BaseLayout>
